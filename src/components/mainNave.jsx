import logo from '../assets/logo-footer.svg' ; 
const Logo = () => {
  return (
    <img
      src={logo}
      alt="Digital Store Logo"
      width={253}
      height={44}
    />
  );
};

export default Logo;